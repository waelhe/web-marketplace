# وثيقة العمارة — web-marketplace (واجهة السوق عبر BFF)

> هذه الوثيقة تصف النظام كما هو مقيسًا لا كما يُظنّ: كل رقم وكل مسار وكل
> سلوك فيها إمّا مقيس مباشرة على الإنتاج (Railway) أو مستند إلى الوثائق
> الرسمية المحزومة داخل `node_modules/next/dist/docs` (Next.js 16.3.5)
> وإلى ملفات الحقيقة في مستودع الباك اند (app-java-v3 @ `c94866c`).
> تاريخ القياس المرجعي: 2026-09-22.

---

## 1. ما هذا النظام؟ (الصورة الكاملة)

**web-marketplace** هو واجهة الويب العربية (RTL) للسوق، وهي ليست «صفحات
تقرأ API» بل **BFF كامل**: خادم Next.js يحمل سرَّ عميل OAuth2 وينفّذ تدفق
authorization-code + PKCE بنفسه. الرموز (access/refresh) تبقى في الخادم
داخل كوكي الحساب الموقّع؛ المتصفح لا يرى رمزًا قط — كوكي الجلسة المشفّر
فقط. الباك اند (app-java-v3، مستودع مستقل) هو **الأصل والمرجع**: تسجيل
العميل، ونطاقات الأدوار، وأعمار الرموز، وحق الإبطال — كلها بيده.

المكدس المثبّت (مقيس 2026-09-15، وأعيد تثبيته هنا على أداة Node مثبتة
خارج المسار العام `../.tools/node-v26.8.2-linux-x64` بما يطابق قاعدة
«الأدوات المثبتة فقط»):

| الطبقة | الاختيار | لماذا (قياسًا) |
|---|---|---|
| الإطار | Next.js 16.3.5 (App Router, Turbopack) | الوثائق المحزومة هي المرجع — هذه ليست نسخة Next التي في ذاكرة التدريب |
| UI | React 19.3 + TypeScript 5 | إلزامي للإطار |
| المصادقة | Better Auth 1.7.5 + Generic OAuth (عديم قاعدة بيانات) | الحالة عديمة DB موثّقة رسميًا؛ SQLite استُبعد بفشل بناء مقيس |
| العقدة | Node v26.8.2 | مثبّتة في `engines` و`.npmrc engine-strict` |
| التنسيق | بدون إطار CSS — رموز تصميم منطقية (logical properties) + خط Cairo متغيّر | قرار مؤسِّس موثّق في README |

## 2. قنوات البيانات الأربع (كلها مقيسة حيًّا)

النظام يصل إلى الباك اند عبر أربع قنوات منفصلة عمدًا، ولكل واحدة عقدتها:

1. **RSC مصادَق** — `src/lib/api/server.ts`: مكوّنات الخادم تجلب البيانات
   مباشرة من `BACKEND_URL` (دليل BFF المحزوم يمنع الاستجلاب الذاتي عبر
   Route Handlers الخاصة). حامل Bearer يُستخرج من كوكي الحساب
   (`useAccountCookie: true` — مسار DB-less الرسمي في better-auth 1.7.5)
   مع تجديد تلقائي. الإخفاقات المتوقعة تعود كبيانات لا كاستثناءات.
2. **RSC عام** (جديدة في هذا التسليم) — `src/lib/api/public.ts`: جلب
   مجهول (بلا جلسة وبلا Bearer) للأسطح العامة في الكتالوج، لأن هذه
   الأسطح تجيب GET مجهولًا (مقيس على الإنتاج): `/api/v1/listings`
   و`/api/v1/listings/{id}`. هذه هي القناة التي تراها محركات البحث.
3. **متصفح → وسيط** — `src/app/api/backend/[...path]/route.ts` +
   `src/lib/api/client.ts`: ترحيل Bearer للبيانات التي يجب أن تعيش في
   العميل؛ 401 مع `reauth: true` هو إشارة إعادة المصادقة، والأخطاء تُفكّ
   من `application/problem+json` (RFC 7807/9457 بعقد الباك اند نفسه).
4. **المصادقة** — `src/app/api/auth/[...all]/route.ts`: معالج Better Auth
   شاملًا ردّ نداء OAuth (`/api/auth/callback/marketplace-web` — سلسلة
   مسجّلة حرفيًا في الباك اند، قاعدة RFC 9700 §4.1.3).

**عقد العدّ (L40):** قراءة تفاصيل إعلان واحدة في الباك اند تُحتسب مشاهدة
(مكرَّرة رقميًّا ببصمة IP). لذلك `getListingDetail` ملفوفة بـ
`React cache()` حتى تشارك `generateMetadata` وجسم الصفحة **جلبًا واحدًا
لكل طلب** — جلبان كانا سيضاعفان العدّ.

## 3. أسطح الواجهة الحالية

| المسار | النوع | ما يحدث |
|---|---|---|
| `/` | عام + واعٍ بالجلسة | حالة الدخول + مدخل التصفّح العام + مدخل الحارات |
| `/listings` | عام | تصفّح الإعلانات النشطة (صفحات؛ `/api/v1/listings`) |
| `/listings/[id]` | عام | تفاصيل الإعلان: `generateMetadata` (عنوان بقالب الجذر + canonical + OpenGraph) + تضمين JSON-LD كما ألّفه الباك اند حرفيًا |
| `/neighborhoods` | عام | اختيار الحارة (المرحلة 2): تنقّل `?parent=` + بحث `?q=` (أرضية حرفين) على شجرة geo الإدارية عبر `/api/v1/geo/{id}/children` و`/api/v1/geo/suggest`؛ ورقة الحيّ (مستوى 3) تحمل زر الانضمام (Server Action) للمسجّل، وبوابة الدخول للمجهول. **لا يُبنى على `/geo/tree`**: يجيب 409 CONFLICT-001 على الإنتاج (مقيس ٣ مرات) — واقع خلفي مُصرّح |
| `/neighborhood` | مصادَق | حارتي: المجهول → بوابة تسجيل الدخول (noindex — لا شيء مجتمعي عام، عقد 401 المقيس)؛ بلا عضوية → مدخل الاختيار؛ العضو → بطاقة العضوية (L41) + تغذية الحارة بتصفية الفئات وصفحات (L42) + مؤلّف المنشورات |
| `/profile` | مصادَق | الجلسة (DAL) + جلب `/me` المباشر |
| `/api/auth/[...all]` | خادم | معالج Better Auth |
| `/api/backend/[...path]` | خادم | ترحيل Bearer للعميل |

**كتابات المجتمع (أول مسار تحويلات في الواجهة):** Server Actions في
`src/app/neighborhood/actions.ts` (انضمام/مغادرة/نشر) — النمط الرسمي
المحزوم: حدود الإطار (POST فقط + فحص Origin/Host) + فحص الجلسة داخل كل
action («بوابة العرض ليست حدًا أمنيًا») + الباك اند هو سلطة التصريح
(401/403/404). الكتابة تركب `backendSend` — نفس قناة server.ts
مباشرة (لا استجلاب ذاتي عبر الوسيط). عقد الأنواع المشترك النقي
(`community-contract.ts`) يفصل المفردات عن القناة كي تستوردها نماذج
العميل بلا `next/headers` في حزمة المتصفح (خطأ بناء مقيس ومُصلح).

**السلوك 404 (مقيس وموثّق):** معرف مجهول/مؤرشف → واجهة not-found مع
`<meta name="robots" content="noindex"/>` وحالة HTTP 200 لأن حدَّ
`loading.tsx` الجذري يبدأ البث أولًا. هذا هو العقد الرسمي الموثّق في دليل
`loading` المحزوم: «لا يؤدي إلى فهرسة» لأن `noindex` صريح. الحصول على 404
حقيقي يتطلب إزالة حدّ التحميل (شاشة فارغة 0.6–2.7 ث لكل جولة Railway) —
مُسجّل كقرار مقصود بعدم فعله.

## 4. عقد SEO — الواجهة هي الطرف المُلزَم (L39)

الخلفية: الباك اند يملك قدرة SEO كاملة لكنه **ينتظرنا**:

- `marketplace.catalog.seo.listing-path` افتراضيه **`/listings/{id}`** —
  أي أن مسار هذه الواجهة هو العقد نفسه؛ JSON-LD `url` ومدخلات
  `sitemap.xml` تُؤلَّف ضدّه.
- `sitemap.xml` على الإنتاج يجيب حاليًا 503: «القدرة مطفأة: لا أصل موقع
  عام مربوط (`marketplace.catalog.seo.public-site-base-url`)». تفعيلها
  **خطوة في الباك اند**: ربط الأصل بـ
  `https://web-marketplace-production-5cc1.up.railway.app`.
- كتلة JSON-LD (`RealEstateListing` النمطية: name/description/url/
  dateModified/offers/address) توجد فقط للإعلانات ذات كتلة عقار (L31)؛
  تضمَّها الواجهة **حرفيًا** كما أرسلت (الدليل المحزوم: وسم
  `<script type="application/ld+json">` أصلي مع تهريب `<` إلى `\u003c`).
- `metadataBase` في الجذر يعيد استخدام `BETTER_AUTH_URL` — حقيقة أصل
  واحدة من متغير بيئة واحد، في التطوير والإنتاج.

**حالة التحقق الحي (صراحة):** قالب العنوان وcanonical وog:title/og:url
مقيسة حيًّا على إعلان إنتاج حقيقي — على خادم dev **وعلى نشر الإنتاج
نفسه** (قياس متصفح حقيقي بعد الدفع، 2026-09-22: canonical يحمل أصل
الإنتاج). **فرع JSON-LD نفسه لم يقس حيًّا بعد**: الإنتاج يحوي إعلانًا
واحدًا (`property: null` → `jsonLd: null`) — الفرع ينتظر أول إعلان
عقاري بكتلة عقار.

## 5. الباك اند كما هو فعلًا: 22 وحدة (لا 9 نوافذ)

البنية الحقيقية لـ app-java-v3 @ `c94866c` — عقد Maven واحد بـ 22 وحدة،
قياسًا مباشرًا للمستودع (وعدّ الشروح `@*Mapping` يعطي **125 توصيلة مسار
في 32 مراقبًا**؛ دخان الباك اند الرسمي قاس 108 مسارات في سلسلة L44 —
الرقمان يقيسان شيئين مختلفين: الشروح مقابل المسارات المدخّنة):

- **الهيكل**: `app` (التجميع) · `platform-infra` · `shared` (عقد API
  المشتركة: DTOs، problem+json، الأحداث)
- **النواة التجارية**: `catalog` · `booking` · `payments` · `pricing` ·
  `ledger` · `availability` · `reviews`
- **الهوية والمحتوى**: `provider` · `identity` · `media` · `disputes`
- **الاكتشاف والعقار**: `search` · `geo` (L0–L3؛ **الحارة = مستوى 3**) ·
  `realestate` · `ai`
- **التواصل**: `messaging` · `notifications`
- **المجتمع**: `community` — كان غائبًا عن الوصف القديم وهو حيّ في
  الإنتاج (القسم 6)
- **البوابة**: `edge` — بوابة BFF بجافا (القسم 8)

الطبقات المتراكمة L0→L46 هي «سجلّ دَين مسدَّد»: كل طبقة خطة + بوابات +
قياس وصول إلى الإنتاج موثّق في ملفات الحقيقة (`docs/release/`، خطط
`docs/*.md`). قاعدة المعرفة `.ua/knowledge-graph.json` عند التوليد
`6f70886f`: **4050 عقدة / 5582 حافة**.

## 6. طبقة المجتمع L41–L46 (قلب Nextdoor — كلها في الإنتاج)

| الطبقة | ما هي | حال الوصول للإنتاج |
|---|---|---|
| L41 | عضوية الحارة (المرساة): الانتساب لـ geo مستوى 3 | PR #331 → `b40246e` |
| L42 | المنشورات والتغذية والتعليقات | PR #336 → `38a141fa` |
| L46 | **جسر العقار المجتمعي**: تفعيل إعلان عقار → إشعار تلقائي لسكان الحارة نفسها (حدث لكل عضو نشط، بلا إشعار للناشر نفسه) | PR #340 → `02903d9` |
| L45 | الإشراف: تقارير المحتوى + طابور الحسم الإداري | PR #342 → `e61b246` |
| L44 | الرسائل المباشرة بين الجيران (بلا مواعيد؛ زوج قياسي + فهرس فريد) | PR #353 → `3d1fde3` |
| L43 | فئة التوصيات (طلب/عرض في التغذية) | PR #354 → `c9a32f7` |

نموذج الخصوصية (مقيس): كل أسطح المجتمع تتطلب مصادقة — كـ Nextdoor؛
الأسطح العامة = الكتالوج والمراجعات والبحث وgeo وصفحات المزوّدين العامة
+ `robots.txt`/`sitemap.xml` يخرجهما الباك اند بنفسه.

## 7. الرؤية: خمسة منتجات في نظام واحد

«أريد شيئًا يجمع yelp, nextdoor and nextdoor business, airbnb» —
الباك اند الحالي يحمل الأربعة كأسطح جاهزة، والواجهة هي المستهلك الناقص:

| المنتج | ما يلبه في الباك اند | المستهلك في الواجهة |
|---|---|---|
| **Airbnb** | catalog + booking + payments + availability + pricing | **مبدوء هنا**: `/listings` + `/listings/[id]` (تفاصيل/SEO) — الحجز والدفع لاحقًا (مفاتيح Stripe بيد المالك) |
| **Nextdoor** | community L41–L46 (خصوصية الحارة) | المرحلة 2 من خارطة الواجهة |
| **Nextdoor Business** | جسر L46: إعلان مزوّد يصل جيرانه تلقائيًا | صفحة التفاصيل هي **نقطة إطلاق الجسر** — أول صفحة تجلس في تقاطع العقار/الحارة |
| **Yelp** | المراجعات + صفحات المزوّدين العامة + البحث الجغرافي | مرحلة لاحقة (ثاني سطح SEO) |

الفجوات المبوّبة (محرّكة ببوابات، لا تُدفن): G-N4 دليل الأعمال، G-N2
التحقق من السكان، G-N6 المجموعات، ومفاتيح Stripe للدفع.

## 8. قرار التعايش مع بوابة Java (edge) — لا إلغاء، ولا تسلسل

الباك اند دمج بوابة BFF بجافا (`marketplace-edge`, PR #356 → `fb61f5b`):
عميل OAuth2 سري + TokenRelay (على `/api/**` فقط) + جلسات Redis +
`csrf.spa`، وتُنشر **كخدمة Railway ثانية** (`edge:8081 → app:8080` عبر
الشبكة الداخلية `railway.internal`؛ IaC + Dockerfile مستقل #360، إصلاح
معرّف الكاش #364، وعقد الفحص الصحي `${PORT:8081}` + liveness/readiness
#366 — بانتظار أمر تنفيذ النشر من مالك الباك اند وفق §14.3 عنده).

**القرار (مستند إلى وثائق حوكمة الباك اند نفسها):**

- وثيقة `client-hosting-strategy-plan.md` في الباك اند **تعدّد صراحةً
  خادم Next.js كشكل استضافة مشروع** (النمط 1)، وفرضية «الفرع B» عندها
  تنصّ: إن أمسك العميلُ بخادمه → يُرفع TokenRelay وتنحدر البوابة إلى
  مسارٍ صامت. أي أن **تعايشنا هو التصميم المُخطَّط عندهم، لا استثناء**.
- «الباك اند هو الأصل والواجهة تابعة لا تُلغي الباك اند» — محقَّق
  بنيويًا في هذا المستودع: تسجيل العميل (clientId سرّه عندنا لكن سطر
  التسجيل عندهم)، PKCE، الأدوار، أعمار الرموز، وحق الإبطال — كلها
  قرراتٍ يتخذها الباك اند. بوابتنا **عميل مسجَّل تابع، بلا حقيقة ذاتية**.
- **التسلسل (بوابة → بوابتنا → الباك اند) مرفوض قياسًا**: جلسات Java
  المتسلسلة لا يقرؤها Node؛ النقر المزدوج يضاعف الكمون؛ وأي تعميم
  مصادقة على الواجهة (`anyRequest().authenticated()`) يقتل سطح SEO
  العام الذي بُني هذا التسليم لأجله.
- **متى تحلّ بوابة Java محلّ بوابتنا؟** في حالة واحدة: إعادة كتابة
  الواجهة كـ SPA خالص بلا خادم (كل شيء في المتصفح). ما دمنا نملك خادمًا
  (SSR/SEO/جلسات) فبوابة Next.js هي الشكل المعتمد والبوابة الجافية
  تعمل موازية على سطحها الخاص.
- قرار Redis النهائي عندهم (سجل الحقيقة #363): Upstash المُدار مع قبول
  مخاطرة الفقد الكامل في الطبقة المجانية (التخفيف: إعادة مصادقة صامتة
  SSO). رفض JDBC رسميًا لغياب مرجعية رسمية. لا يعنينا مباشرة إلا عبر
  جلسات البوابة الجافية عند نشرها.

## 9. خارطة طريق الواجهة (ست مراحل بأسماء صريحة)

الأولوية من «الخلطة»: ابدأ بما يفتح SEO والجسر، ثم المجتمع (أكبر عدم
تناظر: 6 طبقات خلفية بلا مستهلك)، ثم رأس المال التجاري:

1. **تفاصيل الإعلان + التصفّح** — جوهر Airbnb ونقطة إطلاق جسر L46 وأول
   كتلة SEO. **= التسليم الأول** (`/listings` + `/listings/[id]` +
   قناة البيانات العامة + هذه الوثيقة؛ دُفع ونُشر وقيس على الإنتاج).
2. **أساس المجتمع** — اختيار الحارة (geo) → العضوية (L41) → التغذية
   والتعليقات والتوصيات (L42/L43): أكبر قيمة غير مستهلَكة في الباك اند.
   **= التسليم الثاني** (`/neighborhoods` + `/neighborhood` +
   انضمام/مغادرة/نشر عبر Server Actions؛ دُفع بكلمة صريحة 2026-09-22
   ونشرته Railway تلقائيًا وقُيست أسطحه على الإنتاج نفسه — §10).
   التعليقات كل واحدة (L42) تنتظر فجوة خلفية مقيسة — انظر §10.
3. **مسار المزوّد** (Nextdoor Business) — إنشاء/إدارة الإعلانات،
   الاكتمال (L38)، جسر التفعيل (L46) من جهة الناشر.
4. **الصندوق** — الإشعارات + رسائل الجيران (L44)، بالاستطلاع أولًا.
5. **السمعة** — صفحة المزوّد العامة + المراجعات: ثاني سطح SEO.
6. **الحجز والدفع** — بانتظار مفاتيح Stripe (بوابة المالك).

> ملاحظة تاريخية صريحة: مرحلتا «تصفّح الإعلانات» و«البحث التفاعلي»
> السابقتان (التزمان المحليان `a42a04a`/`5099c70` من جلسته سابقة) **لم
> يصلوا إلى الأصل** — أعيد بناء أساس التصفّح هنا من `origin/main`
> (`3eb353b`) بعقد مقيس من الكود الحي، وأعيدت كتابة وثيقة العمارة من
> الصفر لتشمل ما كانت ستحمله وما بعده (22 وحدة، المجتمع، الرؤية، قرار
> التعايش). إن بقيا على جهاز آخر عند المالك فالتقاطع ممكن ويُحلّ عند
> الدمج. أما هذا التسليم نفسه (`19b8fa4`) فقد دُفع إلى الأصل بكلمة
> صريحة من المالك (2026-09-22)، ونشرته Railway تلقائيًا، وقُيست أسطحه
> على الإنتاج نفسه (القسم 10).

## 10. واقع التحقق (ما قيس وما لم يقس — بلا ادعاء)

**مقيس حيًّا في هذا التسليم** (خادم dev بالأداة المثبتة v26.8.2، و
`agent-browser` 0.38.1، و`/_next/mcp`):

- **نشر الإنتاج حي ومقيس (2026-09-22، بعد الدفع بكلمة صريحة):**
  Railway نشرت `19b8fa4` تلقائيًا (الربط GitHub/main/auto-deploy
  الموثّق في AGENTS.md)؛ `/listings` و`/listings/[id]` تخدم على
  `web-marketplace-production-5cc1.up.railway.app` ببيانات الإنتاج
  الحقيقية، وقياس متصفح حقيقي على صفحة التفاصيل الإنتاجية أعطى العنوان
  بقالب الجذر وcanonical بأصل الإنتاج وog:title، والنقر الفعلي
  تصفّح→تفاصيل يعمل — بنفس القياسات على dev.
- `get_compilation_issues` → `{"issues":[]}` (نظيف) و`get_routes` يعدّ
  `/listings` و`/listings/[id]`.
- `/listings` يعرض إعلان الإنتاج الحقيقي («إعلان تحقق هيكلي — e2e»،
  1,000.00 ر.س.) — بيانات Railway حيّة.
- النقر على البطاقة ينتقل فعليًا إلى `/listings/{id}` (انتظار URL
  بالضبط) والصفحة تعرض السعر/الضيوف/الوصف/«آخر تحديث» — بلا أخطاء
  متصفح.
- HTML التفاصيل يحمل: `title` بقالب الجذر، `canonical`، `og:title`،
  `og:url`، و`lang="ar" dir="rtl"`.
- `npm run build` أخضر (القاعدة) + `tsc --noEmit` + eslint نظيفان.
- سطح API العام للباك اند مقيس من هنا: تصفّح/تفاصيل/robots.txt حية؛
  `sitemap.xml` يعطي 503 (قدرة مطفأة — تفعيلها بيد الباك اند).
- سلوك مقيس بلا لبس: بعد تنقّل داخلي (client-side) إلى التفاصيل يكتمل
  تحديث `document.title`/head بعد ~ثانية من networkidle (بثّ metadata
  مع جلب بعيد ~2.8 ث)؛ التحميل الكامل للصفحة مكتمل فور networkidle،
  وHTML الخدمة مكتمل دائمًا — الزواحف (تحميل كامل) غير متأثرة،
  و`get_errors` نظيف طوال القياس.
- **أسطح المرحلة 2 مقيسة حيًا (2026-09-22، نفس الجولات):**
  `/neighborhoods` يعرض شجرة الإنتاج الحقيقية من الجذر المثبت
  (ريف دمشق → قدسيا → قدسيا البلد/ضاحية قدسيا/الهامة) عبر
  `/geo/{id}/children`؛ البحث (قد) يعيد نتائج الإنتاج عبر
  `/geo/suggest`؛ النقر التفاعلي (انتظار العنصر ثم النقرة) يمرّر
  التنقّل للمستوى الثالث مع عرض الأحياء الثلاثة وبوابات الدخول
  للمجهول؛ `/neighborhood` للمجهول يعرض بوابة الخصوصية بـ noindex
  (لا نداء خلفيًا أساسًا — البوابة قبل أي جلب)؛ `get_compilation_issues`
  نظيف و`get_routes` يعدّ المسارين؛ `npm run build` أخضر
  (٨ مسارات، الاثنان الجديدان ديناميكيان) + tsc + eslint نظيفان.
  المقاييس: ttfb ‏53.5ms‏، lcp ‏752ms‏، CLS ‏0‏، إماهة ‏4.6ms‏ (المُنتقي).
- **نشر الإنتاج للمرحلة 2 حيّ ومقيس (بعد الدفع بكلمة صريحة
  2026-09-22):** Railway نشرت `4e34ae0` تلقائيًا؛ فحوص HTTP على
  `web-marketplace-production-5cc1.up.railway.app` كلها 200
  (الجذر والتنقّل والبحث وبوابة حارتي و/listings)، والتنقّل
  التفاعلي بمتصفح حقيقي على الإنتاج مرّ كاملًا (ريف دمشق → قدسيا
  → الأحياء الثلاثة ببوابات الدخول) وبوابة حارتي تعرض بلا أي مكوّن
  عميل مُواه (تقديم خادم خالص — zero client JS للبوابة) و
  CLS ‏0‏ وttfb ‏202.4ms‏.

**غير مقيس / معلَن بصراحة:**

- فرع JSON-LD وكتلة العقار في صفحة التفاصيل: الإنتاج بلا إعلان عقاري
  (إعلان واحد، `property: null`) — الفرع مطبّق على العقد لكنه ينتظر بيانات.
- **مسارات المرحلة 2 المصادَقة (انضمام/مغادرة/نشر/قراءة التغذية)**: تنقصها
  جلسة حقيقية — نفس القيد المعلَن لتدفق الدخول (سرّ عميل موضعي + حساب
  خلفي). العقد مقيس من المصدر والمصافحة المجهولة على الإنتاج (401
  AUTHN-001 على الأسطح الثلاثة)، والفروع تعمل على مستوى الأنواع
  والتجميع، لكن **لم يقس أي مسار كتابة من طرف مستخدم حقيقي** — لا
  ادعاء.
- **فجوة خلفية مقيسة (تخصّ مالك الباك اند):** لا يوجد مسار عام لقراءة
  منشور واحد (`GET /posts/{id}`) — واجهات التعليقات (`GET/POST
  /posts/{postId}/comments`) بلا سياق المنشور نفسه؛ لذلك أُرجئت أسطح
  التعليقات إلى ما بعد إضافة القراءة المفردة (أو أي سطح خلفي مكافئ).
- **حادثة `/geo/tree` الخلفية:** 409 CONFLICT-001 «Resource was modified
  by another transaction. Please retry.» مستمرة على الإنتاج (٣ محاولات)
  بينما `/children` و`/suggest` سليمان — الواجهة تُبنى على الصالح
  والمُسار (findGeoNodeById) جاهز للتبديل لحالة واحدة عند الإصلاح.
- تدفق الدخول الكامل (نموذج/موافقة/كوكي): يتطلب سر عميل OAuth حقيقيًا
  وحساب مستخدم خلفي — بيئة هذه الجلسة تحمل قيمًا موضعية في `.env.local`
  (لا أسرار). لم يُدّعَ.
- `/actuator/health` على الإنتاج يجيب **DOWN (503)** بينما
  `liveness` و`readiness` كلاهما **UP** (قياس المجموعات مباشرة) —
  البوليغة المجمّعة تناقض مجموعاتها؛ ملاحظة تُرفع لمالك الباك اند،
  لا تمنع أسطحنا.
- الحالة 200-مع-noindex للمعرفات المجهولة (القسم 3): عقد بثّ موثّق،
  مقيس، وقرار مقصود.

## 11. صيانة هذه الوثيقة

- أي تعديل بنيوي (قناة بيانات، سطح، قرار تعايش) يُحدَّث هنا في نفس
  الالتزام الذي يغيّر الكود — الوثيقة بلا الكود كذب، والكود بلا الوثيقة
  دين.
- مصادر الحقيقة بالترتيب: (1) الكود الحي والمتصفح، (2) الوثائق المحزومة
  `node_modules/next/dist/docs`، (3) ملفات حقيقة الباك اند
  (`docs/release/`، خطط `docs/`)، (4) وثائق الشبكة بنفس الإصدار عند
  غياب المحزوم — لا ذاكرة التدريب.
